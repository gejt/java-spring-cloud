server:
  port: 3301

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gejt/my-cloud-config
          username:
          password:

